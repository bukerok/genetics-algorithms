var config = require('./config.json');

var i;

// creating initial population
var population;

for (i = 0; i < config.iterations; i++) {
    // selection
    // selecting parents
    // crossover
    // mutation
}
